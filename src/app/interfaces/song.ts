export interface Song {
    title    : string,
    album   : string,
    cover   : string,
    primary : string,
    secondary : string,
    hangul  : Array<string>,
    rom     : Array<string>,
    english : Array<string>
}
